#include<iostream>
using namespace std;
int main()
{
    string s={'a','b','c'};
   int output = 0;
  for (int i = 0; i < s.length; i++) {
  output = output * 10;
  output = output + parse(string[i]) //parse first character into a single digit
                                }
}
